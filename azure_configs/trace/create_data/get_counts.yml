logging:
  level: INFO
  path: ./outputs/logs

paths:
  ## INPUTS
  data: researcher-data:AKK/shared/MEDS/TRACE/v02/data
  ## OUTPUTS
  counts: researcher-data:AKK/shared/MEDS/TRACE/v02/code_counts/train # Save counts to this folder
splits: [train]
