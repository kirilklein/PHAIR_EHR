logging:
  level: INFO
  path: ./outputs/logs/causal

paths:
  ## INPUTS
  finetune_model: ./outputs/causal/experiments/{{EXPERIMENT_NAME}}/models/bert

  ## OUTPUTS
  calibrated_predictions: ./outputs/causal/experiments/{{EXPERIMENT_NAME}}/models/bert/calibrated
