logging:
  level: INFO
  path: ./outputs/logs/causal

paths:
  ## INPUTS
  finetune_model: ./outputs/causal/experiments/{{EXPERIMENT_NAME}}/models/baseline

  ## OUTPUTS
  calibrated_predictions: ./outputs/causal/experiments/{{EXPERIMENT_NAME}}/models/baseline/calibrated
